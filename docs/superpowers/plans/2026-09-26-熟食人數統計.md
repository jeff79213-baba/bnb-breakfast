# 熟食人數統計（橫幅第二行）實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 主頁面熟食警示橫幅由單行改為兩行，第二行顯示「熟食總人數（大人／小孩）」與「未食用人數（大人／小孩）」。

**Architecture:** 在 `core.js` 新增純函式 `computeHotPeople(rooms, statusOf)`，規則完全對齊既有的 `computeHotPending`（同樣用 `isHotRoom()` 判定熟食、同樣排除 `isNoMeal()` 不用餐），額外加總 `adult` 與 `child + infant`。`app.js` 的 `render()` 呼叫它，把橫幅內容改以兩個 `<span>`（`.hot-banner-main` / `.hot-banner-sub`）寫入；`style.css` 把 `.hot-banner` 改成 flex 欄向佈局。

**Tech Stack:** 原生 JS（無框架、無建置步驟）。測試框架為 Node 內建 `node:test`。

## Global Constraints

- 測試指令固定為 `node --test tests\core.test.js`（勿用 vitest，本專案不適用）。
- 語法檢查：`node --check app.js`、`node --check core.js`。
- `sw.js` 的 `CACHE_NAME` 每次改版需更版（本次 → `bnb-breakfast-v19-hot-people`）。
- 每次功能修改後 commit；最後 push 部署（GitHub Pages 自動）。
- 熱食判定一律用 `core.js` 的 `isHotRoom(r)`，**不得**改用 `r.breakfastType === 'hot'`（那是 `app.js:200` 篩選與 `app.js:1192` 備料區的既有口徑，本案不處理）。
- 「小孩」定義 = `child + infant`（對齊 `app.js:186 orderKid()`）。
- 不得改動 `index.html` 的 `#hotAlertBanner` 元素結構（維持空元素、內容由 JS 寫入）。
- 不得改動橫幅的點擊行為（`app.js:1371-1373`：僅 `alert` 狀態觸發 `currentFilter = 'hot-pending'`）。
- 不得改動 `computeStats` 與 `computeHotPending` 本體。
- fixture 房號使用字串（`'101'`）。
- 專案為台灣繁體中文，介面文案全部使用繁體中文。
- 專案 localStorage 前綴為 `rnb_`，本案不新增任何 storage key。

---

### Task 1: core.js 新增 computeHotPeople（純函式）＋測試

**Files:**
- Modify: `core.js:53`（於 `computeHotPending` 函式結束後新增 `computeHotPeople`）
- Modify: `core.js:149-153`（return 物件加入 `computeHotPeople`）
- Test: `tests/core.test.js`（尾端追加 7 個 test）

**Interfaces:**
- Consumes: `isHotRoom(r)`（core.js:30）、`isNoMeal(r)`（core.js:41）——兩者皆為 core.js 內既有函式，無需新增。
- Produces: `computeHotPeople(rooms, statusOf)` → `{ total: {adult, kid, people}, pending: {adult, kid, people} }`
  - `rooms`: room 陣列（可為 `undefined`）
  - `statusOf`: `(room) => string`，回傳 `'completed'` 或其他值
  - `adult` = `Number(r.adult) || 0`；`kid` = `(Number(r.child) || 0) + (Number(r.infant) || 0)`；`people` = `adult + kid`
  - `total` = 全部熟食房（排除不用餐）；`pending` = 其中 `statusOf(r) !== 'completed'` 者
  - Task 2 會讀 `hotPeople.total.people`、`total.adult`、`total.kid`、`pending.people`、`pending.adult`、`pending.kid`

- [ ] **Step 1: 寫失敗測試（追加到 tests/core.test.js 尾端）**

```js
test('computeHotPeople: 基本加總（熟食總人數 = 大人 + 小孩）', () => {
  const rooms = [
    { roomNumber: '101', source: '官網', adult: 2, child: 1, infant: 0 },
    { roomNumber: '308', source: 'Agoda', eggMilk: '加購', adult: 1, child: 2, infant: 0 },
  ];
  const out = C.computeHotPeople(rooms, () => 'pending');
  assert.deepStrictEqual(out.total, { adult: 3, kid: 3, people: 6 });
  assert.deepStrictEqual(out.pending, { adult: 3, kid: 3, people: 6 });
});

test('computeHotPeople: 小孩 = child + infant（訂單模式三欄）', () => {
  const rooms = [{ roomNumber: '201', source: '官網', adult: 2, child: 1, infant: 2 }];
  const out = C.computeHotPeople(rooms, () => 'pending');
  assert.strictEqual(out.total.adult, 2);
  assert.strictEqual(out.total.kid, 3);
  assert.strictEqual(out.total.people, 5);
});

test('computeHotPeople: 已用餐的房間整間扣除（total 不變、pending 減）', () => {
  const rooms = [
    { roomNumber: '101', source: '官網', adult: 2, child: 1, infant: 1 },
    { roomNumber: '308', source: 'Agoda', eggMilk: '加購', adult: 1, child: 0, infant: 0 },
  ];
  const oneDone = C.computeHotPeople(rooms, (r) => (r.roomNumber === '101' ? 'completed' : 'pending'));
  assert.deepStrictEqual(oneDone.total, { adult: 3, kid: 2, people: 5 });
  assert.deepStrictEqual(oneDone.pending, { adult: 1, kid: 0, people: 1 });
  const allDone = C.computeHotPeople(rooms, () => 'completed');
  assert.deepStrictEqual(allDone.total, { adult: 3, kid: 2, people: 5 });
  assert.deepStrictEqual(allDone.pending, { adult: 0, kid: 0, people: 0 });
});

test('computeHotPeople: 排除不用餐的房間（isNoMeal）', () => {
  const rooms = [
    { roomNumber: '101', source: '官網', adult: 2, child: 1, mealTime: '' },
    { roomNumber: '102', source: '手動訂單', adult: 3, child: 3, mealTime: '不用餐' },
  ];
  const out = C.computeHotPeople(rooms, () => 'pending');
  assert.deepStrictEqual(out.total, { adult: 2, kid: 1, people: 3 });
  assert.deepStrictEqual(out.pending, { adult: 2, kid: 1, people: 3 });
});

test('computeHotPeople: 排除非熟食房（平台房、不加購房）', () => {
  const rooms = [
    { roomNumber: '101', source: '官網', adult: 2, child: 1 },
    { roomNumber: '312', source: 'Booking.com', adult: 4, child: 4 },
    { roomNumber: '507', source: '官網', vegan: '不加購', adult: 5, child: 5 },
  ];
  const out = C.computeHotPeople(rooms, () => 'pending');
  assert.deepStrictEqual(out.total, { adult: 2, kid: 1, people: 3 });
  assert.deepStrictEqual(out.pending, { adult: 2, kid: 1, people: 3 });
});

test('computeHotPeople: 空／undefined 輸入回傳全 0', () => {
  const expected = {
    total: { adult: 0, kid: 0, people: 0 },
    pending: { adult: 0, kid: 0, people: 0 },
  };
  assert.deepStrictEqual(C.computeHotPeople([], () => 'pending'), expected);
  assert.deepStrictEqual(C.computeHotPeople(undefined, () => 'pending'), expected);
});

test('computeHotPeople: 舊純房號模式（無人數欄位）回傳全 0', () => {
  const rooms = [{ roomNumber: '101', source: '官網', vegan: '', eggMilk: '' }];
  const out = C.computeHotPeople(rooms, () => 'pending');
  assert.deepStrictEqual(out.total, { adult: 0, kid: 0, people: 0 });
  assert.deepStrictEqual(out.pending, { adult: 0, kid: 0, people: 0 });
});
```

- [ ] **Step 2: 執行使測試失敗**

Run: `node --test tests\core.test.js`
Expected: FAIL，錯誤訊息為 `TypeError: C.computeHotPeople is not a function`（7 個新測試失敗，既有 14 個通過）。

- [ ] **Step 3: 實作 computeHotPeople**

於 `core.js` 中 `computeHotPending` 函式結束（`core.js:53` 的 `}`）之後、`computeStats` 之前新增：

```js
  function computeHotPeople(rooms, statusOf) {
    const zero = () => ({ adult: 0, kid: 0, people: 0 });
    const total = zero();
    const pending = zero();
    for (const r of (rooms || [])) {
      if (!isHotRoom(r)) continue;
      if (isNoMeal(r)) continue;
      const adult = Number(r.adult) || 0;
      const kid = (Number(r.child) || 0) + (Number(r.infant) || 0);
      total.adult += adult; total.kid += kid; total.people += adult + kid;
      if (statusOf(r) !== 'completed') {
        pending.adult += adult; pending.kid += kid; pending.people += adult + kid;
      }
    }
    return { total, pending };
  }
```

- [ ] **Step 4: 加入 core.js 的 return 物件**

將 `core.js:149-153` 的

```js
  return {
    HOT_SOURCES, isHotSource, isNoAdd, isAddon, isHotRoom, isPlatformRoom,
    isNoMeal, computeHotPending, computeStats, normalizeSlots, addSlot,
    enabledSlots, sortSlots, timeOf, groupKeyOf, assignGroupColors, sortRoomsGrouped
  };
```

改為

```js
  return {
    HOT_SOURCES, isHotSource, isNoAdd, isAddon, isHotRoom, isPlatformRoom,
    isNoMeal, computeHotPending, computeHotPeople, computeStats, normalizeSlots,
    addSlot, enabledSlots, sortSlots, timeOf, groupKeyOf, assignGroupColors,
    sortRoomsGrouped
  };
```

- [ ] **Step 5: 執行使測試通過**

Run: `node --check core.js`
Expected: 無錯誤輸出。

Run: `node --test tests\core.test.js`
Expected: 全數 PASS，既有 14 + 新增 7 = **21** 個測試。

- [ ] **Step 6: Commit**

```bash
git add core.js tests/core.test.js
git commit -m "feat: core 新增 computeHotPeople（熟食人數統計）"
```

---

### Task 2: 橫幅改兩行（style.css + app.js render）

**Files:**
- Modify: `style.css:96-116`（`.hot-banner` 區塊）
- Modify: `app.js:223-233`（`render()` 內的熟食警示橫幅）

**Interfaces:**
- Consumes: `C.computeHotPeople(rooms, statusOf)`（Task 1 產出）、`C.computeHotPending`（既有）、`s.hot`（既有 `computeStats` 結果，`app.js:211` 的 `s`）。
- Produces: DOM 結構 `#hotAlertBanner` > (`span.hot-banner-main` [, `span.hot-banner-sub`])。次行僅在 `hotPeople.total.people > 0` 時存在。

- [ ] **Step 1: style.css 改 flex 欄向佈局 + 新增 main/sub 樣式**

將 `style.css:96-116` 的

```css
/* ===== 熱食警示橫幅 ===== */
.hot-banner {
  width: 100%; margin-top: 12px;
  border: none; border-radius: 14px;
  padding: 16px;
  font-size: 19px; font-weight: 800;
  text-align: center;
  box-shadow: 0 3px 10px rgba(232,89,12,.35);
}
```

改為

```css
/* ===== 熱食警示橫幅 ===== */
.hot-banner {
  width: 100%; margin-top: 12px;
  border: none; border-radius: 14px;
  padding: 16px;
  text-align: center;
  box-shadow: 0 3px 10px rgba(232,89,12,.35);
  display: flex; flex-direction: column; align-items: center; gap: 4px;
}
.hot-banner-main { font-size: 19px; font-weight: 800; line-height: 1.25; }
.hot-banner-sub { font-size: 12px; font-weight: 600; opacity: .85; line-height: 1.4; }
```

`.hot-banner.alert`、`.hot-banner.ok`、`.hot-banner:active`、`@keyframes pulse` 四段**維持原樣不動**。

- [ ] **Step 2: app.js render() 改寫橫幅內容**

將 `app.js:223-233` 的

```js
  // 熟食警示橫幅（簡約線條）
  const banner = $('hotAlertBanner');
  const hotPending = C.computeHotPending(state.rooms, r => getStatus(tk, r.roomNumber));
  banner.classList.remove('hidden');
  if (hotPending > 0) {
    banner.textContent = `尚有 ${hotPending} 間熟食未用餐`;
    banner.className = 'hot-banner alert';
  } else {
    banner.textContent = s.hot > 0 ? '熟食已全數用餐完成' : '今日尚無熟食房號';
    banner.className = 'hot-banner ok';
  }
```

改為

```js
  // 熟食警示橫幅（主行：未用餐間數／次行：熟食與未食用人數）
  const banner = $('hotAlertBanner');
  const hotPending = C.computeHotPending(state.rooms, r => getStatus(tk, r.roomNumber));
  const hotPeople = C.computeHotPeople(state.rooms, r => getStatus(tk, r.roomNumber));
  banner.classList.remove('hidden');
  let bannerMain;
  if (hotPending > 0) {
    bannerMain = `尚有 ${hotPending} 間熟食未用餐`;
    banner.className = 'hot-banner alert';
  } else {
    bannerMain = s.hot > 0 ? '熟食已全數用餐完成' : '今日尚無熟食房號';
    banner.className = 'hot-banner ok';
  }
  let bannerSub = '';
  if (hotPeople.total.people > 0) {
    const t = hotPeople.total, p = hotPeople.pending;
    bannerSub = `熟食 ${t.people} 人（大人${t.adult} 小孩${t.kid}）　未食用 ${p.people} 人（大人${p.adult} 小孩${p.kid}）`;
  }
  banner.innerHTML = `<span class="hot-banner-main">${bannerMain}</span>`
    + (bannerSub ? `<span class="hot-banner-sub">${bannerSub}</span>` : '');
```

注意：
- `bannerMain` 與 `bannerSub` 內所有插值都是經 `Number() || 0` 轉換後的數字或固定中文字串，無使用者輸入，故**不需** `escapeHtml`。
- `s.hot > 0` 這個判斷**維持原樣不可改**：舊純房號模式 `total.people` 為 0，但 `s.hot` 仍 > 0，若誤改會讓有熟食房號的舊資料誤顯示「今日尚無熟食房號」。
- `bannerSub` 開頭的 `　` 是全形空格（U+3000），不可用半形空格取代。

- [ ] **Step 3: 驗證**

Run: `node --check app.js`
Expected: 無錯誤輸出。

Run: `node --test tests\core.test.js`
Expected: 21 個測試全數 PASS（Task 1 的成果不得被破壞）。

- [ ] **Step 4: 手動瀏覽器驗證（本機預覽）**

在專案根目錄執行靜態伺服器並開啟頁面：

```powershell
python -m http.server 8080
```

接著開 `http://localhost:8080`，逐項確認：

1. 匯入訂單模式資料（I~M 欄）→ 橫幅第二行顯示 `熟食 N 人（大人A 小孩B）　未食用 M 人（大人C 小孩D）`
2. 點掉一間房的用餐狀態 → 該房大人小孩從「未食用」扣除、「熟食」總數不變、間數同步減 1
3. 全部用餐完成 → 橫幅轉綠底，次行 `未食用 0 人（大人0 小孩0）`
4. 匯入 8 欄模式資料（有 `adult`/`child` 無 `infant`）→ 小孩數字正確
5. 用舊純房號資料（無 adult/child 欄）→ 橫幅不出現次行，且主行仍正確顯示「熟食已全數用餐完成」而非「今日尚無熟食房號」
6. 點橫幅 → 篩選結果與點擊範圍與改動前一致（僅 alert 狀態可點）
7. 390px 寬度確認次行為單行；若換行成兩行，**將 `.hot-banner-sub` 的 `font-size` 由 `12px` 改為 `11px`** 後重新確認（此為唯一允許的字級調整）
8. 320px 寬度確認次行自然換行且不溢出、不裁切

關閉伺服器：停止該 `python` 行程。

- [ ] **Step 5: Commit**

```bash
git add style.css app.js
git commit -m "feat: 熟食橫幅新增第二行（熟食/未食用 大人小孩人數）"
```

---

### Task 3: sw.js 更版 + 全量驗證 + push 部署

**Files:**
- Modify: `sw.js:2`

- [ ] **Step 1: 更版 CACHE_NAME**

將 `sw.js:2` 的

```js
const CACHE_NAME = 'bnb-breakfast-v18-sort-name';
```

改為

```js
const CACHE_NAME = 'bnb-breakfast-v19-hot-people';
```

- [ ] **Step 2: 全量驗證**

Run: `node --check app.js`
Expected: 無錯誤輸出。

Run: `node --check core.js`
Expected: 無錯誤輸出。

Run: `node --test tests\core.test.js`
Expected: 21 個測試全數 PASS。

Run: `node --check sw.js`
Expected: 無錯誤輸出。

- [ ] **Step 3: Commit**

```bash
git add sw.js
git commit -m "chore: sw 快取更版 v19（熟食人數統計）"
```

- [ ] **Step 4: Push 部署**

```bash
git push origin main
```
Expected: push 成功，GitHub Pages 自動部署完成。

---

## Self-Review Check

- **Spec coverage**：
  - spec A（`computeHotPeople` 純函式與五條規則）→ Task 1 Step 3/4，7 個測試對應 spec 測試策略第 1~7 項
  - spec B（橫幅兩行版面、次行文字格式、320px 換行）→ Task 2 Step 1/2，手動驗證第 1/7/8 項
  - spec C（三種狀態呈現、`total.people === 0` 隱藏次行）→ Task 2 Step 2，手動驗證第 3/5 項
  - spec D（render 整合、`computeHotPending` 維持不動）→ Task 2 Step 2
  - spec「範圍外的項目」四項 → Global Constraints 明列（不統一 breakfastType 口徑、不新增 stat card、不改 index.html 結構、不動點擊行為）
  - spec 部署（sw.js v19 + push）→ Task 3
- **Placeholder scan**：無 TBD/TODO；所有程式碼、測試碼、命令、預期輸出均完整給出。
- **Type consistency**：`computeHotPeople` 在 Task 1 定義並 export，Task 2 以 `C.computeHotPeople` 呼叫；回傳欄位 `total.people` / `total.adult` / `total.kid` / `pending.people` / `pending.adult` / `pending.kid` 兩處名稱一致；CSS class `.hot-banner-main` / `.hot-banner-sub` 在 Task 2 Step 1 定義、Step 2 使用，名稱一致。
