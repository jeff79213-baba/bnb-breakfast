# 房號與早餐 — 分類/平台改名/訂單分色/用餐時間 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 將房號與早餐系統改為「來源＋加購」分類、右欄改名「平台」、訂單編號分色、並新增「早餐用餐時間」常駐時段與時間欄快速選擇（含不用餐扣除熟食未用餐統計）。

**Architecture:** 抽出 `core.js`（瀏覽器全域 + Node CommonJS 雙可用）放置純函式並以 `node --test` 測試；`app.js` 呼叫 core 函式取代現有的 `orderInHot`/`isHotMeal`/`isNormalMeal`；設定與時間選擇以新 modal 呈現；訂單編號於匯入時反查並以 border-bottom 色帶分色。

**Tech Stack:** Vanilla JS（無框架）、XLSX (CDN)、Node 內建 `node:test`、localStorage（`rnb_data_v1`）。

## Global Constraints

- 不得引入框架或新依賴；測試僅用 Node 內建 `node --test`（不放 package.json，直接 `node --test tests/`）。
- localStorage 命名一律保留 `rnb_data_v1`、前綴 `rnb_`，不得改。
- 分類規則（唯一真理表）：`不加購→平台`、`加購→熟食`、`來源含手動訂單/官網/官網訂單→熟食`、其餘→平台。
- 熟食（hot）統計含加購房；熟食＋平台＝總數。
- 「不用餐」＝ `room.mealTime === '不用餐'`，該房不計入「尚有 X 間熟食未用餐」。
- 右欄標題與空狀態「一般」→「平台」；**統計卡片標籤「一般」不改**。
- 所有 render 皆以 `escapeHtml` 跳脫使用者可輸入內容（時段文字、orderId 不需顯示，故無跳脫需求）。
- 維持行動優先、離線（service worker）不變；改 `sw.js` 需更版 `CACHE_NAME`。

---

### Task 1: 建立 core.js 純函式模組 ＋ 單元測試

**Files:**
- Create: `core.js`
- Create: `tests/core.test.js`

**Interfaces:**
- Produces（後續任務皆依賴此 API，簽名不可改）：
  - `isHotSource(src) → boolean`
  - `isNoAdd(r) → boolean`
  - `isAddon(r) → boolean`
  - `isHotRoom(r) → boolean`
  - `isPlatformRoom(r) → boolean`
  - `isNoMeal(r) → boolean`
  - `computeHotPending(rooms, statusOf) → number`（`statusOf(r)` 回傳 `'pending'|'completed'`）
  - `computeStats(rooms, statusOf) → { hot, normal, addon, completed, pending, total }`
  - `normalizeSlots(slots) → string[]`
  - `addSlot(slots, label) → {label,enabled}[]`
  - `enabledSlots(slots) → string[]`
  - `groupKeyOf(r) → string`
  - `assignGroupColors(rooms, palette) → { roomKey: color }`（roomKey 用的是 `r.roomNumber`）
- 瀏覽器以全域 `window.BreakfastCore`；Node 以 `require('./core.js')`。

- [ ] **Step 1: 撰寫失敗測試 `tests/core.test.js`**

```js
'use strict';
const { test } = require('node:test');
const assert = require('node:assert');
const C = require('../core.js');

const hotSrcRoom = { roomNumber: '101', source: '官網', vegan: '', eggMilk: '' };
const platformRoom = { roomNumber: '312', source: 'Booking.com', vegan: '', eggMilk: '' };
const addonRoom = { roomNumber: '308', source: 'Agoda', vegan: '', eggMilk: '加購' };
const noAddRoom = { roomNumber: '507', source: '官網', vegan: '不加購', eggMilk: '' };
const noMealRoom = { roomNumber: '102', source: '手動訂單', vegan: '', eggMilk: '', mealTime: '不用餐' };

test('isHotSource: 手動/官網/官網訂單為熟食來源，其餘不是', () => {
  assert.strictEqual(C.isHotSource('手動訂單'), true);
  assert.strictEqual(C.isHotSource('官網訂單'), true);
  assert.strictEqual(C.isHotSource('官網'), true);
  assert.strictEqual(C.isHotSource('Booking.com'), false);
  assert.strictEqual(C.isHotSource(''), false);
  assert.strictEqual(C.isHotSource(undefined), false);
});

test('isAddon: eggMilk 含加購即為真', () => {
  assert.strictEqual(C.isAddon(addonRoom), true);
  assert.strictEqual(C.isAddon({ eggMilk: '蛋奶加購' }), true);
  assert.strictEqual(C.isAddon(platformRoom), false);
  assert.strictEqual(C.isAddon(undefined), false);
});

test('isNoAdd: vegan === 不加購', () => {
  assert.strictEqual(C.isNoAdd(noAddRoom), true);
  assert.strictEqual(C.isNoAdd(platformRoom), false);
});

test('isHotRoom / isPlatformRoom: 真理表', () => {
  assert.strictEqual(C.isHotRoom(hotSrcRoom), true);     // 官網 → 熟食
  assert.strictEqual(C.isHotRoom(platformRoom), false);  // Booking → 平台
  assert.strictEqual(C.isHotRoom(addonRoom), true);      // Agoda+加購 → 熟食
  assert.strictEqual(C.isHotRoom(noAddRoom), false);     // 官網但不加購 → 平台
  assert.strictEqual(C.isPlatformRoom(platformRoom), true);
  assert.strictEqual(C.isPlatformRoom(hotSrcRoom), false);
});

test('isNoMeal: mealTime === 不用餐', () => {
  assert.strictEqual(C.isNoMeal(noMealRoom), true);
  assert.strictEqual(C.isNoMeal(hotSrcRoom), false);
});

test('computeHotPending: 熟食區未用餐且扣除不用餐', () => {
  const rooms = [hotSrcRoom, platformRoom, addonRoom, noAddRoom, noMealRoom];
  const statusOf = () => 'pending'; // 全部未用餐
  // 熟食 = 101, 308；207 平台、507 不加購(平台)、102 不用餐(扣除)
  assert.strictEqual(C.computeHotPending(rooms, statusOf), 2);
  const allDone = () => 'completed';
  assert.strictEqual(C.computeHotPending(rooms, allDone), 0);
});

test('computeStats: 熟食含加購、熟食+平台=總數', () => {
  const rooms = [hotSrcRoom, platformRoom, addonRoom, noAddRoom];
  const statusOf = (r) => (r.roomNumber === '101' ? 'completed' : 'pending');
  const s = C.computeStats(rooms, statusOf);
  assert.strictEqual(s.hot, 2);        // 101(官網) + 308(加購)
  assert.strictEqual(s.normal, 2);     // 312 + 507(不加購)
  assert.strictEqual(s.addon, 1);      // 308
  assert.strictEqual(s.completed, 1);
  assert.strictEqual(s.pending, 3);
  assert.strictEqual(s.total, 4);
});

test('normalizeSlots / addSlot / enabledSlots: 時段操作', () => {
  assert.deepStrictEqual(C.normalizeSlots([' 8:15 ', '', ' 9:30']), ['8:15', '9:30']);
  assert.deepStrictEqual(C.normalizeSlots(undefined), []);

  let slots = C.addSlot([], '8:15');
  slots = C.addSlot(slots, '8:15');       // 重複不加
  slots = C.addSlot(slots, ' 9:30 ');     // 去空白
  slots = C.addSlot(slots, '');           // 空不加
  assert.deepStrictEqual(slots, [{ label: '8:15', enabled: true }, { label: '9:30', enabled: true }]);

  const mixed = [{ label: '8:15', enabled: true }, { label: '9:30', enabled: false }, { label: ' 10:00 ', enabled: true }];
  assert.deepStrictEqual(C.enabledSlots(mixed), ['8:15', '10:00']);
  assert.deepStrictEqual(C.enabledSlots([]), []);
});

test('assignGroupColors: 依 orderId 分組、空 orderId 退 source、穩定', () => {
  const palette = ['#e64980', '#9775fa', '#4dabf7'];
  const rooms = [
    { roomNumber: '106', orderId: 'A', source: '官網' },
    { roomNumber: '301', orderId: 'A', source: '官網' },
    { roomNumber: '508', orderId: 'A', source: '官網' },
    { roomNumber: '312', orderId: '', source: '短租' },
    { roomNumber: '507', orderId: '', source: '短租' },
    { roomNumber: '101', orderId: '', source: 'Booking.com' },
  ];
  const m = C.assignGroupColors(rooms, palette);
  assert.strictEqual(m['106'], m['301']);  // 同訂單同色
  assert.strictEqual(m['301'], m['508']);
  assert.strictEqual(m['312'], m['507']);  // 同來源同色
  assert.notStrictEqual(m['312'], m['101']);
  const m2 = C.assignGroupColors(rooms, palette);
  assert.strictEqual(m['106'], m2['106']); // 穩定
  assert.strictEqual(m['312'], m2['312']);
});
```

- [ ] **Step 2: 執行確認失敗**

Run: `node --test tests/`
Expected: FAIL，`Cannot find module '../core.js'`。

- [ ] **Step 3: 建立 `core.js`**

```js
'use strict';

/* =========================================================
   core.js — 純函式（分類 / 統計 / 時段 / 分色）
   瀏覽器：window.BreakfastCore；Node：require('./core.js')
   ========================================================= */
(function (root, factory) {
  const api = factory();
  if (typeof module === 'object' && module.exports) module.exports = api;
  else root.BreakfastCore = api;
})(typeof globalThis !== 'undefined' ? globalThis : this, function () {

  const HOT_SOURCES = ['手動訂單', '官網', '官網訂單'];

  function isHotSource(src) {
    const s = String(src == null ? '' : src).trim();
    return HOT_SOURCES.some(h => s.includes(h));
  }

  function isNoAdd(r) {
    return !!(r && r.vegan === '不加購');
  }

  function isAddon(r) {
    return !!(r && /加購/.test(String(r.eggMilk || '')));
  }

  function isHotRoom(r) {
    if (!r) return false;
    if (isNoAdd(r)) return false;
    return isAddon(r) || isHotSource(r.source);
  }

  function isPlatformRoom(r) {
    return !isHotRoom(r);
  }

  function isNoMeal(r) {
    return !!(r && r.mealTime === '不用餐');
  }

  function computeHotPending(rooms, statusOf) {
    let n = 0;
    for (const r of (rooms || [])) {
      if (!isHotRoom(r)) continue;
      if (isNoMeal(r)) continue;
      if (statusOf(r) !== 'completed') n++;
    }
    return n;
  }

  function computeStats(rooms, statusOf) {
    let hot = 0, normal = 0, addon = 0, completed = 0;
    for (const r of (rooms || [])) {
      if (isAddon(r)) addon++;
      if (isHotRoom(r)) hot++;
      else normal++;
      if (statusOf(r) === 'completed') completed++;
    }
    return { hot, normal, addon, completed, pending: rooms.length - completed, total: rooms.length };
  }

  function normalizeSlots(slots) {
    if (!Array.isArray(slots)) return [];
    return slots.map(s => String(s == null ? '' : s).trim()).filter(Boolean);
  }

  function addSlot(slots, label) {
    const list = Array.isArray(slots) ? slots.slice() : [];
    const clean = String(label == null ? '' : label).trim();
    if (!clean) return list;
    if (list.some(x => x && String(x.label || '').trim() === clean)) return list;
    list.push({ label: clean, enabled: true });
    return list;
  }

  function enabledSlots(slots) {
    if (!Array.isArray(slots)) return [];
    return slots.filter(x => x && x.enabled && String(x.label || '').trim())
      .map(x => String(x.label).trim());
  }

  function groupKeyOf(r) {
    if (r && r.orderId && String(r.orderId).trim()) return 'order:' + String(r.orderId).trim();
    return 'src:' + String(r && r.source ? r.source : '').trim();
  }

  function assignGroupColors(rooms, palette) {
    const map = {};
    let i = 0;
    for (const r of (rooms || [])) {
      const k = groupKeyOf(r);
      if (!(k in map)) { map[k] = palette[i % palette.length]; i++; }
    }
    return map;
  }

  return {
    HOT_SOURCES, isHotSource, isNoAdd, isAddon, isHotRoom, isPlatformRoom,
    isNoMeal, computeHotPending, computeStats, normalizeSlots, addSlot,
    enabledSlots, groupKeyOf, assignGroupColors
  };
});
```

- [ ] **Step 4: 執行確認通過**

Run: `node --test tests/`
Expected: 全部 tests PASS（10 個）。

- [ ] **Step 5: Commit**

```bash
git add core.js tests/core.test.js
git commit -m "feat: 新增 core.js 純函式（分類/統計/時段/分色）與單元測試"
```

---

### Task 2: index.html 加入 core.js、選單按鈕與兩個新 modal

**Files:**
- Modify: `index.html`

**Interfaces:**
- Consumes: Task 1 core.js 已存在，需於 app.js 之前載入。
- Produces（供 Task 4/7/8 使用的 DOM id）：
  - button `#menuSlotsBtn`
  - `#mealSlotsModal`, `#slotsInput`, `#slotsAddBtn`, `#slotsList`, `#slotsCloseBtn`
  - `#timePickerModal`, `#timePickerTitle`, `#timePickerOptions`, `#timePickerCloseBtn`

- [ ] **Step 1: 在 index.html 的 scripts 區載入 core.js**

將：
```html
<script src="./app.js"></script>
```
改為：
```html
<script src="./core.js"></script>
<script src="./app.js"></script>
```

- [ ] **Step 2: 功能選單新增「早餐用餐時間」按鈕**

在 `<button class="btn list-btn" id="menuPrepBtn">🧮 備料區</button>` 之後加入：
```html
<button class="btn list-btn" id="menuSlotsBtn">🍳 早餐用餐時間</button>
```

- [ ] **Step 3: 新增用餐時段設定 modal（放在 prepModal 之後）**

```html
<!-- 早餐用餐時間設定 -->
<div class="modal-backdrop hidden" id="mealSlotsModal">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <h2>🍳 早餐用餐時間</h2>
    <p class="hint">時段勾選後會常駐，作為主頁面時間欄的選項。</p>
    <div class="add-row">
      <input type="text" id="slotsInput" inputmode="text" placeholder="例如 8:15">
      <button class="btn primary" id="slotsAddBtn">新增</button>
    </div>
    <div class="manage-list" id="slotsList"></div>
    <button class="btn subtle full" id="slotsCloseBtn">完成</button>
  </div>
</div>
```

- [ ] **Step 4: 新增時間選擇 modal（放在 mealSlotsModal 之後）**

```html
<!-- 時間欄位快速選擇 -->
<div class="modal-backdrop hidden" id="timePickerModal">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <h2 id="timePickerTitle">用餐時間</h2>
    <div class="manage-list" id="timePickerOptions"></div>
    <button class="btn subtle full" id="timePickerCloseBtn">取消</button>
  </div>
</div>
```

- [ ] **Step 5: 手動確認 HTML 結構**

開啟 `index.html`，確認：core.js 在 app.js 之前載入、選單有 5 個功能按鈕、兩個新 modal 顯示正常（backdrop 有 `hidden`）。

- [ ] **Step 6: Commit**

```bash
git add index.html
git commit -m "feat: 新增用餐時間設定/時間選擇 modal 與選單按鈕、載入 core.js"
```

---

### Task 3: app.js 套用 core 分類與統計（含平台改名、不用餐扣除、orderId 保留）

**Files:**
- Modify: `app.js`

**Interfaces:**
- Consumes: Task 1 core API、Task 2 DOM ids。
- Produces: `state.settings.mealSlots`（Task 4/5 使用）、雙模式分類渲染都改由 `isHotRoom` 決定。

- [ ] **Step 1: 全域呼叫 core（新增於 `const $ = ...` 附近）**

在 `const $ = (id) => document.getElementById(id);` 之後加：
```js
const C = window.BreakfastCore;
```

- [ ] **Step 2: defaultState / loadState 加入 mealSlots**

`defaultState()` 中 `settings` 物件改為：
```js
settings: {
  types: JSON.parse(JSON.stringify(DEFAULT_TYPES)),
  prep: Object.assign({}, DEFAULT_PREP),
  mealSlots: []
},
```
`loadState()` 的 settings 回傳值改為：
```js
settings: {
  types: Array.isArray(data.settings?.types) && data.settings.types.length ? data.settings.types : base.settings.types,
  prep: Object.assign({}, DEFAULT_PREP, (data.settings && data.settings.prep) || {}),
  mealSlots: Array.isArray(data.settings?.mealSlots) ? data.settings.mealSlots : []
},
```

- [ ] **Step 3: sanitizeRooms 保留 orderId（三個分支都加）**

- `'infant' in r` 分支的物件加 `orderId: r.orderId || ''`
- `'adult' in r` 分支的物件加 `orderId: r.orderId || ''`
- `else` 分支的物件加 `orderId: ''`

- [ ] **Step 4: computeStats 改用 core 並移除舊分類函式**

將 `computeStats(dateKey)` 整段替換：
```js
function computeStats(dateKey) {
  return C.computeStats(state.rooms, r => getStatus(dateKey, r.roomNumber));
}
```
刪除整組舊函式：`isHotSource`（app.js:167 附近，注意此函式只有下列函式用到）、`orderIsAddon` 下方的 `orderIsNoAdd`、`orderInHot`、`orderKid`、`orderTotal` 若不再被用到則一併刪除；`mealTypeChoice`/`updateMealTypeButtons`/`mealTypeValue` 保留（mealEditModal 使用）。
此外 `isBreakfast8Mode()`、`isAddon`、`isHotMeal`、`isNormalMeal`（app.js:297-299 附近）替換為：
```js
function isAddon(r) { return C.isAddon(r); }
function isHotMeal(r) { return C.isHotRoom(r); }
function isNormalMeal(r) { return C.isPlatformRoom(r); }
```
> 保留這三個包裝函式的名稱，因 renderGrid 8 欄程式碼大量引用；兩欄篩選邏輯照舊：`hotList = all.filter(r => isHotMeal(r) || isAddon(r))` 等於 `all.filter(C.isHotRoom)`（isAddon 為 isHotRoom 子集），`normalList = all.filter(isNormalMeal)`。

- [ ] **Step 5: render() 使用 core computeStats 與 computeHotPending**

`render()` 中：
```js
const s = computeStats(tk);
```
維持（computeStats 已改 core）。將警示橫幅整段（`const banner = $('hotAlertBanner');` 到 `banner.classList.toggle('hidden', empty);` 之前）的 hotPending 計算改為：
```js
const hotPending = C.computeHotPending(state.rooms, r => getStatus(tk, r.roomNumber));
```

- [ ] **Step 6: 兩欄標題「一般」→「平台」、空狀態改名**

搜尋 `>一般</div>`（renderOrderGrid 與 renderGrid 8 欄各一處）改為 `>平台</div>`；搜尋 `無一般</td>`（兩處）改為 `無平台</td>`。

- [ ] **Step 7: 驗證統計正確性（改動後手動跑單元測試）**

Run: `node --test tests/`
Expected: 仍全數 PASS（core 未動）。再用瀏覽器手動：匯入檔案後確認「熟食」數字含加購、「一般」標題變「平台」、字串「不用餐」的房不計入頂部警示。

- [ ] **Step 8: Commit**

```bash
git add app.js
git commit -m "feat: app.js 改用 core 分類統計、平台改名、不用餐扣除、保留 orderId"
```

---

### Task 4: 訂單編號反查（匯入時）

**Files:**
- Modify: `app.js`

**Interfaces:**
- Consumes: Task 3 的 `sanitizeRooms` 已保留 `orderId`。
- Produces:
  - `parseOrderMatrix` 輸出的每筆 row 帶 `orderId`（有則填）
  - `collectOrderIds(matrix) → Map<roomNo, orderId>`（掃描單一 sheet）
  - `parseExcelFile` 在回傳前把 orderIdMap 套到所有 rows
  - `confirmImport` 兩個分支皆保留 `orderId`

- [ ] **Step 1: parseOrderMatrix 加入訂單編號欄與輸出**

在 `parseOrderMatrix` 中，於 `const srcCol = findCol(/訂單來源/, 12);` 之後加：
```js
const orderCol = findCol(/訂單編號/, -1);
```
其中 `findCol` 目前回傳 fallback；補一個不會回傳 -1 的保護：將 `findCol` 定義改為遇 -1 時回傳該 fallback（已有），並在取值處做守衛。`rec` 物件（`const rec = {...}`）加入 `orderId: orderCol >= 0 ? String(r[orderCol] || '').trim() : ''`。

- [ ] **Step 2: 新增 collectOrderIds 純函式（放在 parseOrderMatrix 之後）**

```js
// 反查：同一筆訂單多間房 → Map<房號, 訂單編號>
function collectOrderIds(matrix) {
  if (!matrix || !matrix.length) return null;
  const norm = matrix.map(row => (Array.isArray(row) ? row : [row]).map(c => String(c ?? '').trim()));
  let hr = -1;
  for (let i = 0; i < Math.min(5, norm.length); i++) {
    if (norm[i].some(c => c.includes('訂單編號')) && norm[i].some(c => /房型房號|房間房號/.test(c))) { hr = i; break; }
  }
  if (hr === -1) return null;
  const header = norm[hr];
  const orderIdx = header.findIndex(c => c.includes('訂單編號'));
  const roomIdx = header.findIndex(c => /房型房號|房間房號/.test(c));
  if (orderIdx < 0 || roomIdx < 0) return null;
  const map = new Map();
  for (let i = hr + 1; i < norm.length; i++) {
    const cell = (norm[i][roomIdx] || '').trim();
    const orderId = (norm[i][orderIdx] || '').trim();
    if (!cell || !orderId) continue;
    cell.split(/[\/／、\n]+/).map(s => s.trim()).filter(Boolean).forEach(seg => {
      const m = seg.match(/^(\d+)\s*([Nn])?/);
      if (m) map.set(m[1] + (m[2] ? 'N' : ''), orderId);
    });
  }
  return map.size ? map : null;
}
```

- [ ] **Step 3: parseExcelFile 掃描所有 sheet 套用 orderIdMap**

在 `parseExcelFile` 的 for 迴圈（含 parseOrderMatrix 的迴圈）之後、`return` 之前，加：
```js
  // 訂單編號反查：以第一份含 訂單編號+房型房號 的 sheet 為準，套用到所有 rows
  for (const name of wb.SheetNames) {
    const m = XLSX.utils.sheet_to_json(wb.Sheets[name], { header: 1, defval: '', raw: false, blankrows: false });
    const m2 = collectOrderIds(m);
    if (m2) {
      parsed.rows.forEach(r => { if (!r.orderId && m2.has(r.roomNumber)) r.orderId = m2.get(r.roomNumber); });
      break;
    }
  }
```
注意：`parsed` 可能來自 parseOrderMatrix（內部已帶 orderId）、parseBreakfastMatrix 或 parseFromMatrix。此步驟於三個 return 前統一執行：把上面區塊放在 `parseExcelFile` 中「先掃新訂單格式（I~M）」迴圈失敗、取得 `parsed8`、以及最後 `return parseFromMatrix(matrix)` 各自 return 前皆需套用，或改為在 `parseExcelFile` 底層重組：將現有各 return 改為先存入區域變數、最後統一套用後 return。請重構成單一出口，套用以下程式碼（置於 function 尾端 return 前）：
```js
  let parsed = null;
  // ...（保留現有解析流程，但把每個 return parsedX 改為 parsed = parsedX; break/跳過）
  if (parsed && parsed.rows) {
    for (const name of wb.SheetNames) {
      const m = XLSX.utils.sheet_to_json(wb.Sheets[name], { header: 1, defval: '', raw: false, blankrows: false });
      const m2 = collectOrderIds(m);
      if (m2) {
        parsed.rows.forEach(r => { if (!r.orderId && m2.has(r.roomNumber)) r.orderId = m2.get(r.roomNumber); });
        break;
      }
    }
  }
  return parsed;
```
> 重構時務必保留原有解析順序：先 parseOrderMatrix 遍歷所有 sheet → 再早餐統計 sheet → 遍歷其他 sheet 的 8 欄 → 退回 parseFromMatrix。

- [ ] **Step 4: confirmImport 兩個分支保留 orderId**

- order 分支 map 物件新增 `orderId: r.orderId || ''`
- 8 欄分支 map 物件新增 `orderId: r.orderId || ''`
- 通用分支 rows 來自 parseFromMatrix，已被 parseExcelFile 套用 map，新增 `orderId: r.orderId || ''`

- [ ] **Step 5: 驗證（用工作區現有 `02訂房記錄.xlsx`）**

以 `node --test tests/` 確認無影響。手動：以 `02訂房記錄.xlsx` 匯入後於瀏覽器 devtools `JSON.stringify(state.rooms[0])` 確認熟食房（如 106）帶對應 OBE 訂單編號。

- [ ] **Step 6: Commit**

```bash
git add app.js
git commit -m "feat: 匯入時反查訂單編號並存入 room.orderId"
```

---

### Task 5: 訂單分色渲染（兩欄網格）

**Files:**
- Modify: `app.js`

**Interfaces:**
- Consumes: Task 1 `assignGroupColors`、Task 4 `orderId` 欄位。
- Produces: 兩欄 row 以 `border-bottom` 色帶標示同訂單/同來源。

- [ ] **Step 1: 定義色盤常數（置於 renderGrid 附近）**

```js
const ORDER_PALETTE = ['#e64980', '#9775fa', '#4dabf7', '#38d9a9', '#ffa94d', '#fa5252', '#82c91e', '#15aabf'];
```

- [ ] **Step 2: renderOrderGrid 套色**

`renderOrderGrid` 內，於 `const all = sortRooms(state.rooms);` 後加：
```js
const colors = C.assignGroupColors(all, ORDER_PALETTE);
```
`mkRow` 內計算 row style 時，`const color = colors[r.roomNumber] || '';`，並把 `<tr ... style="cursor:pointer;...">` 加上 `${color ? 'border-bottom:5px solid ' + color + ';' : ''}`。

- [ ] **Step 3: renderGrid 8 欄模式套色**

`renderGrid` 的 8 欄分支，於 `const all = sortRooms(state.rooms);` 後加：
```js
const colors = C.assignGroupColors(all, ORDER_PALETTE);
```
`mkRow` 內加 `const color = colors[r.roomNumber] || '';` 並在 `<tr ... style="...">` 加入 `${color ? 'border-bottom:5px solid ' + color + ';' : ''}`。

- [ ] **Step 4: 驗證**

`node --test tests/` 全過；瀏覽器：匯入含訂單編號的 excel，確認同單（如 OBE13950332126 的 106/301/508）兩欄下緣同色、跨欄連貫；無 orderId 的短租同來源同色。

- [ ] **Step 5: Commit**

```bash
git add app.js
git commit -m "feat: 同訂單/同來源房號以 border-bottom 色帶標示"
```

---

### Task 6: 用餐時段設定 modal 邏輯

**Files:**
- Modify: `app.js`

**Interfaces:**
- Consumes: Task 2 DOM ids、Task 1 `addSlot`/`enabledSlots`、Task 3 `state.settings.mealSlots`。
- Produces: `renderSlotsList()`、`openSlotsModal()`；`state.settings.mealSlots` 會更新並 `saveState()`。

- [ ] **Step 1: 新增三個函式（放在 openManage 附近）**

```js
function getMealSlots() {
  return Array.isArray(state.settings.mealSlots) ? state.settings.mealSlots : [];
}
function saveMealSlots() {
  state.settings.mealSlots = getMealSlots();
  saveState();
}
function renderSlotsList() {
  const wrap = $('slotsList');
  const slots = getMealSlots();
  if (!slots.length) {
    wrap.innerHTML = '<p class="hint" style="text-align:center;padding:16px 0;">尚未設定時段</p>';
    return;
  }
  wrap.innerHTML = slots.map((s, i) => {
    const label = escapeHtml(String(s.label || '').trim());
    return `<div class="manage-item">
      <label style="display:flex;align-items:center;gap:8px;flex:1;cursor:pointer">
        <input type="checkbox" data-slot-check="${i}" ${s.enabled ? 'checked' : ''}>
        <span class="room-no">${label}</span>
      </label>
      <button class="del-btn" data-slot-del="${i}">✕</button>
    </div>`;
  }).join('');
}
function openSlotsModal() {
  renderSlotsList();
  openModal('mealSlotsModal');
}
```

- [ ] **Step 2: bindEvents 綁定設定 modal**

`$('menuSlotsBtn').addEventListener('click', () => { closeModal('menuModal'); openSlotsModal(); });`
`$('slotsAddBtn').addEventListener('click', () => { const v = $('slotsInput').value; state.settings.mealSlots = C.addSlot(getMealSlots(), v); saveMealSlots(); $('slotsInput').value = ''; renderSlotsList(); toast(v.trim() ? `已新增 ${v.trim()}` : '請輸入時段'); });`
`$('slotsInput').addEventListener('keydown', e => { if (e.key === 'Enter') $('slotsAddBtn').click(); });`
`$('slotsCloseBtn').addEventListener('click', () => closeModal('mealSlotsModal'));`
`$('slotsList').addEventListener('change', e => { const c = e.target.closest('input[data-slot-check]'); if (!c) return; const i = Number(c.dataset.slotCheck); const s = getMealSlots()[i]; if (s) { s.enabled = c.checked; saveMealSlots(); renderSlotsList(); } });`
`$('slotsList').addEventListener('click', e => { const b = e.target.closest('[data-slot-del]'); if (!b) return; const i = Number(b.dataset.slotDel); const s = getMealSlots()[i]; if (!s) return; state.settings.mealSlots = getMealSlots().filter((_, k) => k !== i); saveMealSlots(); renderSlotsList(); toast(`已刪除 ${s.label}`); });`

- [ ] **Step 3: 驗證**

`node --test tests/`；瀏覽器：選單→早餐用餐時間，新增「8:15」「9:30」，重複/空白被擋，勾選/刪除即時反映且重新整理後仍在（localStorage）。

- [ ] **Step 4: Commit**

```bash
git add app.js
git commit -m "feat: 早餐用餐時間設定頁（新增/勾選/刪除，常駐儲存）"
```

---

### Task 7: 時間欄可點與時間選擇彈窗

**Files:**
- Modify: `app.js`

**Interfaces:**
- Consumes: Task 2 `#timePickerModal` DOM、Task 1 `enabledSlots`、Task 3 `state.settings.mealSlots`。
- Produces: `openTimePicker(roomNo)`、`timePickerTarget`；時間格 `data-timeset`。

- [ ] **Step 1: 新增時間彈窗函式（放在 openMealEdit 附近）**

```js
let timePickerTarget = null;
function openTimePicker(roomNo) {
  const room = state.rooms.find(r => r.roomNumber === roomNo);
  if (!room) return;
  timePickerTarget = roomNo;
  $('timePickerTitle').textContent = `房號 ${roomNo} 用餐時間`;
  const slots = C.enabledSlots(state.settings.mealSlots);
  const wrap = $('timePickerOptions');
  let html = '';
  if (slots.length) {
    html += `<div style="display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:10px">` +
      slots.map(s => `<button type="button" class="btn" data-time-value="${escapeHtml(s)}" style="min-height:56px">${escapeHtml(s)}</button>`).join('') + `</div>`;
  } else {
    html += `<p class="hint" style="text-align:center;padding:12px 0">尚未設定時段，請先到選單設定</p>`;
  }
  html += `<button type="button" class="btn" data-time-value="不用餐" style="min-height:52px;width:100%;background:#f1f3f5;border-color:#adb5bd;color:#495057;margin-bottom:10px">🚫 不用餐</button>`;
  html += `<button type="button" class="btn" data-time-value="" style="min-height:52px;width:100%">清除（空白）</button>`;
  wrap.innerHTML = html;
  wrap.querySelectorAll('[data-time-value]').forEach(btn => {
    btn.addEventListener('click', () => {
      const room2 = state.rooms.find(x => x.roomNumber === timePickerTarget);
      if (!room2) return;
      room2.mealTime = btn.dataset.timeValue;
      saveState();
      renderGrid(todayKey());
      closeModal('timePickerModal');
      timePickerTarget = null;
      toast(btn.dataset.timeValue ? `✅ ${room2.roomNumber} 時間：${btn.dataset.timeValue}` : `${room2.roomNumber} 已清除用餐時間`);
    });
  });
  openModal('timePickerModal');
}
```
（`escapeHtml` 於 app.js:547 已有；`state.settings: { mealSlots }` 已由 Task 3 保證存在。）

- [ ] **Step 2: renderOrderGrid 時間格可點**

在 `renderOrderGrid` 的 `mkRow` 中，時間格 td 改為：
```html
<td style="font-size:12px;color:${r.mealTime === '不用餐' ? '#868e96;font-style:italic' : 'inherit'}" data-timeset="${escapeHtml(r.roomNumber)}">${escapeHtml(r.mealTime || '')}${r.mealTime ? '' : '<span class="hint" style="color:#adb5bd">＋</span>'}</td>
```
並保留後方的 `data-orderedit` 改按鈕（其外層容器 td 需移除，改由上面 td 直接含資料），改按鈕如下方維持：
```html
<button data-orderedit="${escapeHtml(r.roomNumber)}" ...>改</button>
```
於 row 的 click 監聽處（該 grid 既有的 `grid.querySelectorAll('tr[data-room]')`），在 `if (e.target.closest('[data-orderedit]')) return;` 與 `[data-orderaddon]`/`[data-orderrevert]` 之後再加上：
```js
if (e.target.closest('[data-timeset]')) return;
```
並新增綁定：
```js
grid.querySelectorAll('[data-timeset]').forEach(td => td.addEventListener('click', (e) => {
  e.stopPropagation();
  openTimePicker(td.dataset.timeset);
}));
```

- [ ] **Step 3: renderGrid 8 欄模式時間格可點**

8 欄 `mkRow` 的時間格：
```html
<td style="font-size:12px;color:${r.mealTime === '不用餐' ? '#868e96;font-style:italic' : 'inherit'}" data-timeset="${escapeHtml(r.roomNumber)}">${escapeHtml(r.mealTime || '')}${r.mealTime ? '' : '<span class="hint" style="color:#adb5bd">＋</span>'}</td>
```
row click 監聽處在 `if (e.target.closest('[data-addon]')) return;` 後加：
```js
if (e.target.closest('[data-timeset]')) return;
```
綁定：
```js
grid.querySelectorAll('[data-timeset]').forEach(td => td.addEventListener('click', (e) => {
  e.stopPropagation();
  openTimePicker(td.dataset.timeset);
}));
```

- [ ] **Step 4: bindEvents 綁定 timePicker 關閉**

`$('timePickerCloseBtn').addEventListener('click', () => { timePickerTarget = null; closeModal('timePickerModal'); });`

- [ ] **Step 5: 驗證**

`node --test tests/`；瀏覽器：新訂單模式與 8 欄模式點時間格皆彈窗；選 8:15 寫入該房；「不用餐」後該房若為熟食且未用餐，頂部警示計數減少、時間格顯示斜體「不用餐」；「清除」回空白；點時間格不會誤觸發整列用餐切換。

- [ ] **Step 6: Commit**

```bash
git add app.js
git commit -m "feat: 時間欄可點選時段/不用餐/清除"
```

---

### Task 8: sw.js 更版 + 全量驗證 + 部署

**Files:**
- Modify: `sw.js`

- [ ] **Step 1: 更版離線快取**

`sw.js` 中 `const CACHE_NAME = 'bnb-breakfast-v7-order-prep';` 改為 `'bnb-breakfast-v8-mealslots'`；確認 `APP_SHELL` 加入 `'./core.js'`。

- [ ] **Step 2: 全量單元測試**

Run: `node --test tests/`
Expected: 全數 PASS。

- [ ] **Step 3: 手動驗證清單**

- [ ] 選單顯示 5 項：管理房號／匯入資料／備料區／早餐用餐時間／匯出今日紀錄
- [ ] 早餐用餐時間：新增 8:15、9:30；重複與空白被擋；勾選狀態重新整理後仍在
- [ ] 匯入 `02訂房記錄.xlsx`：新訂單模式兩欄顯示，右欄標題「平台」
- [ ] 熟食來源（手動/官網）房在左欄；Booking/短租在右欄；加購房在左欄；不加購在右欄
- [ ] 同訂單編號房（如 OBE... 的 106/301/508）下緣同色、跨欄連貫
- [ ] 熟食統計卡＝左欄數（含加購）；熟食+平台=總數
- [ ] 點時間格：可選勾選時段／不用餐／清除；「不用餐」熟食房未用餐時不計入頂部警示
- [ ] 備料區仍可開啟計算（新訂單資料）
- [ ] 匯出今日紀錄正常

- [ ] **Step 4: Commit + Push 部署**

```bash
git add sw.js app.js index.html core.js tests
git commit -m "feat: 早餐用餐時間選項＋分類/平台/訂單分色（上線版）"
git push
```

- [ ] **Step 5: 回報**

依照 AGENTS.md 向使用者回報「已上傳部署完成」，列出 URL（`網址.txt` 或既有 GitHub Pages/Firebase URL）與本次功能摘要。

---

## Self-Review 結果

- **Spec 覆蓋**：A 分類→Task 1+3；B 平台改名→Task 3 Step6；C 訂單分色→Task 4+5；D 統計修正→Task 1+3；E 用餐時間→Task 2+6+7。全部涵蓋。
- **Placeholder 掃描**：無 TODO；每步含可執行指令與完整程式碼。
- **型別一致**：core API 簽名於 Task 1 定義並被 Task 3/5/6/7 依同樣名稱使用（`isHotRoom`、`computeStats`、`computeHotPending`、`addSlot`、`enabledSlots`、`assignGroupColors`）。

- [ ] 計畫完成後依 handoff：以 subagent-driven-development 執行。