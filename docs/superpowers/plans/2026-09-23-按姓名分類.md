# 按姓名分類（訂單模式）實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 訂單模式下新增可切換「房號｜姓名」排序開關，姓名模式於熟食、平台各區塊內將同姓名房排在一起，且不改變蛇形兩欄分佈。

**Architecture:** 在 `core.js` 新增純函式 `sortRoomsGrouped(list)`（同姓名分組、組間依最小房號、組內依房號、無姓名殿後）。於 `renderOrderGrid` 依 `state.settings.sortMode` 選用該函式；頂部統計區新增切換按鈕。

**Tech Stack:** 原生 JS（無框架）。測試框架為 Node 內建 `node:test`。

## Global Constraints

- 測試指令固定為 `node --test tests\core.test.js`（勿用 vitest，本專案不適用）。
- 語法檢查：`node --check app.js`、`node --check core.js`。
- `sw.js` 的 `CACHE_NAME` 每次改版需更版（本次 → `bnb-breakfast-v18-sort-name`）。
- 每次功能修改後 commit；最後 push 部署（GitHub Pages 自動）。
- localStorage storage key 為 `rnb_data_v1`（不新增 key，開關存於 `state.settings.sortMode`）。
- 8 欄模式（`isBreakfast8Mode`）與舊卡片模式不得顯示開關、不得受影響。
- 蛇形分欄邏輯（`renderOrderGrid` 內 `hotList`/`normalList`/`ordered`/`split`/兩欄輸出）一律不動。
- fixture 房號使用字串（`'305'`）。

---

### Task 1: core.js 新增 sortRoomsGrouped（純函式）＋測試

**Files:**
- Modify: `core.js:122-126`（return 物件）
- Modify: `core.js`（新增函式於 `assignGroupColors` 之後）
- Test: `tests/core.test.js`

**Interfaces:**
- Produces: `sortRoomsGrouped(list)` — 輸入 room 陣列，回傳依姓名分組排序後的新陣列（不改原陣列）。規則：有姓名者依姓名分組（組間依各組最小房號小→大、組內依房號小→大）；無姓名（trim 後空）者殿後（依房號小→大）。

- [ ] **Step 1: 寫失敗測試（追加到 tests/core.test.js 尾端）**

```js
test('sortRoomsGrouped: 同姓名相鄰、組間依最小房號、組內依房號', () => {
  const rooms = [
    { roomNumber: '305', guestName: '王小明' },
    { roomNumber: '306', guestName: '陳大文' },
    { roomNumber: '308', guestName: '王小明' },
    { roomNumber: '502', guestName: '王小明' },
    { roomNumber: '201', guestName: '陳大文' },
  ];
  const out = C.sortRoomsGrouped(rooms).map(r => r.roomNumber);
  // 陳大文組最小 201 < 王小明組最小 305 → 陳大文組在前
  assert.deepStrictEqual(out, ['201', '306', '305', '308', '502']);
});

test('sortRoomsGrouped: 無姓名房排最後（依房號）', () => {
  const rooms = [
    { roomNumber: '502', guestName: '王小明' },
    { roomNumber: '305', guestName: '王小明' },
    { roomNumber: '101', guestName: '' },
    { roomNumber: '601', guestName: '   ' },
    { roomNumber: '308', guestName: '王小明' },
  ];
  const out = C.sortRoomsGrouped(rooms).map(r => r.roomNumber);
  assert.deepStrictEqual(out, ['305', '308', '502', '101', '601']);
});

test('sortRoomsGrouped: 多組 + 無姓名混合、結果穩定、空輸入', () => {
  const rooms = [
    { roomNumber: '507', guestName: '' },
    { roomNumber: '305', guestName: '王小' },
    { roomNumber: '502', guestName: '王小' },
    { roomNumber: '201', guestName: '陳大' },
    { roomNumber: '308', guestName: '王小' },
    { roomNumber: '306', guestName: '陳大' },
    { roomNumber: '101', guestName: '' },
  ];
  const expected = ['201', '306', '305', '308', '502', '101', '507'];
  assert.deepStrictEqual(C.sortRoomsGrouped(rooms).map(r => r.roomNumber), expected);
  assert.deepStrictEqual(C.sortRoomsGrouped(rooms).map(r => r.roomNumber), expected); // 穩定
  assert.deepStrictEqual(C.sortRoomsGrouped([]), []);
  assert.deepStrictEqual(C.sortRoomsGrouped(undefined), []);
});
```

- [ ] **Step 2: 執行使測試失敗**

Run: `node --test tests\core.test.js`
Expected: FAIL（`C.sortRoomsGrouped is not a function`）。

- [ ] **Step 3: 實作函式**

於 `core.js` 的 `assignGroupColors` 函式結束後、return 之前新增：

```js
  function roomKeyOf(r) {
    return String(r && r.roomNumber != null ? r.roomNumber : '').trim();
  }

  function roomNumberCmp(a, b) {
    const na = parseInt(a, 10), nb = parseInt(b, 10);
    const bothNum = !isNaN(na) && !isNaN(nb) && String(na) === a && String(nb) === b;
    if (bothNum) return na - nb;
    return a < b ? -1 : a > b ? 1 : 0;
  }

  function sortRoomsGrouped(list) {
    const rooms = (list || []).slice();
    rooms.sort((x, y) => roomNumberCmp(roomKeyOf(x), roomKeyOf(y)));
    const named = new Map();
    const unnamed = [];
    for (const r of rooms) {
      const name = String(r && r.guestName ? r.guestName : '').trim();
      if (!name) { unnamed.push(r); continue; }
      if (!named.has(name)) named.set(name, []);
      named.get(name).push(r);
    }
    const groups = [...named.values()];
    groups.sort((a, b) => roomNumberCmp(roomKeyOf(a[0]), roomKeyOf(b[0])));
    return groups.flat().concat(unnamed);
  }
```

於 return 物件（core.js:122-126）加入：

```js
  return {
    HOT_SOURCES, isHotSource, isNoAdd, isAddon, isHotRoom, isPlatformRoom,
    isNoMeal, computeHotPending, computeStats, normalizeSlots, addSlot,
    enabledSlots, sortSlots, timeOf, groupKeyOf, assignGroupColors, sortRoomsGrouped
  };
```

- [ ] **Step 4: 執行使測試通過**

Run: `node --test tests\core.test.js`
Expected: 全數 PASS（原 11 + 新 3 = 14）。

- [ ] **Step 5: Commit**

```bash
git add core.js tests/core.test.js
git commit -m "feat: core 新增 sortRoomsGrouped（同姓名分組排序）"
```

---

### Task 2: app.js 整合 — 狀態加入 sortMode 並於 renderOrderGrid 選用

**Files:**
- Modify: `app.js:47-58`（`defaultState`）
- Modify: `app.js:60-82`（`loadState`）
- Modify: `app.js:320`（`renderOrderGrid` 排序來源）

**Interfaces:**
- Consumes: `C.sortRoomsGrouped`（Task 1 產出）。
- Produces: `state.settings.sortMode`（`'room' | 'name'`，預設 `'room'`），`renderOrderGrid` 依此選排序。

- [ ] **Step 1: defaultState 加入 sortMode**

`app.js:50-54` 的 settings 物件加一欄：

```js
    settings: {
      types: JSON.parse(JSON.stringify(DEFAULT_TYPES)),
      prep: Object.assign({}, DEFAULT_PREP),
      mealSlots: [],
      sortMode: 'room'
    },
```

- [ ] **Step 2: loadState 合併 sortMode**

`app.js:69-73` 的 settings 物件加一欄（保有舊資料相容）：

```js
      settings: {
        types: Array.isArray(data.settings?.types) && data.settings.types.length ? data.settings.types : base.settings.types,
        prep: Object.assign({}, DEFAULT_PREP, (data.settings && data.settings.prep) || {}),
        mealSlots: C ? C.sortSlots(Array.isArray(data.settings?.mealSlots) ? data.settings.mealSlots : []) : [],
        sortMode: (data.settings && data.settings.sortMode === 'name') ? 'name' : 'room'
      },
```

- [ ] **Step 3: renderOrderGrid 依 sortMode 選排序**

`app.js:320`：

```js
  const all = sortRooms(state.rooms);
```

改為：

```js
  const byRoom = sortRooms(state.rooms);
  const all = state.settings.sortMode === 'name' ? C.sortRoomsGrouped(byRoom) : byRoom;
```

- [ ] **Step 4: 驗證**

Run: `node --check app.js`
Expected: 無錯誤輸出。

- [ ] **Step 5: Commit**

```bash
git add app.js
git commit -m "feat: 訂單模式支援姓名排序（state.settings.sortMode）"
```

---

### Task 3: 頂部排序開關 UI（index.html + style.css + app.js）

**Files:**
- Modify: `index.html:53`（stats-section 內、`.stats-grid` 後新增開關）
- Modify: `style.css`（新增 `.sort-toggle`/`.sort-opt` 樣式）
- Modify: `app.js:233-241`（`render()` 顯示控制）
- Modify: `app.js:1339-1344`（`bindEvents` 綁定點擊）

**Interfaces:**
- Consumes: `state.settings.sortMode`、`isOrderMode()`。
- Produces: `#sortToggle`（含 `data-sort="room | name"` 兩按鈕），點擊後更新 `state.settings.sortMode` → `saveState()` → `render()`。

- [ ] **Step 1: index.html 新增開關**

`index.html` 於 `.stats-grid` 結束（第 51 行 `</div>`）之後、`.progress-wrap` 前插入：

```html
    <div id="sortToggle" class="sort-toggle hidden">
      <span style="font-size:12px;font-weight:700;color:var(--muted)">排序</span>
      <button type="button" class="sort-opt active" data-sort="room">房號</button>
      <button type="button" class="sort-opt" data-sort="name">姓名</button>
    </div>
```

- [ ] **Step 2: style.css 新增樣式**

於 `.progress-wrap` 規則（style.css:75）後新增：

```css
/* ===== 排序方式切換（訂單模式） ===== */
.sort-toggle { display: flex; align-items: center; gap: 6px; margin-top: 10px; }
.sort-opt {
  background: var(--card); border: 1px solid #dee2e6; border-radius: 999px;
  font-size: 12px; font-weight: 700; padding: 5px 14px; color: var(--text);
}
.sort-opt.active { background: var(--text); color: #fff; border-color: var(--text); }
```

- [ ] **Step 3: render() 顯示控制 + 同步 active 狀態**

`app.js` 於 `render()` 內、`renderGrid(tk);`（第 241 行）前插入：

```js
  // 排序方式開關：僅訂單模式顯示
  const sortToggle = $('sortToggle');
  if (sortToggle) {
    sortToggle.classList.toggle('hidden', !isOrderMode());
    const nameMode = state.settings.sortMode === 'name';
    sortToggle.querySelectorAll('.sort-opt').forEach(btn => {
      btn.classList.toggle('active', (btn.dataset.sort === 'name') === nameMode);
    });
  }
```

- [ ] **Step 4: bindEvents 綁定點擊**

`app.js` 於 filterChips 綁定（第 1339-1344 行）後新增：

```js
  $('sortToggle').addEventListener('click', e => {
    const btn = e.target.closest('.sort-opt');
    if (!btn) return;
    state.settings.sortMode = btn.dataset.sort;
    saveState();
    render();
  });
```

- [ ] **Step 5: 驗證**

Run: `node --check app.js`
Expected: 無錯誤輸出。

- [ ] **Step 6: Commit**

```bash
git add index.html style.css app.js
git commit -m "feat: 頂部新增 房號|姓名 排序開關（僅訂單模式）"
```

---

### Task 4: sw.js 更版 + 全測試 + commit + push 部署

**Files:**
- Modify: `sw.js:2`

- [ ] **Step 1: 更版 CACHE_NAME**

`sw.js:2`：

```js
const CACHE_NAME = 'bnb-breakfast-v18-sort-name';
```

- [ ] **Step 2: 全量驗證**

Run: `node --check app.js`
Expected: 無錯誤輸出。

Run: `node --check core.js`
Expected: 無錯誤輸出。

Run: `node --test tests\core.test.js`
Expected: 14 個測試全數 PASS。

- [ ] **Step 3: Commit**

```bash
git add sw.js
git commit -m "chore: sw 快取更版 v18（姓名排序）"
```

- [ ] **Step 4: Push 部署**

```bash
git push origin main
```
Expected: push 成功，GitHub Pages 自動部署完成。

---

## Self-Review Check

- **Spec coverage**：spec A（開關 UI）→ Task 3；spec B（排序規則純函式）→ Task 1；spec C（render 整合）→ Task 2；spec D（`state.settings.sortMode` 持久化）→ Task 2 Step 1/2；spec E（僅訂單模式顯示）→ Task 3 Step 3。
- **Placeholder scan**：無 TBD/TODO，所有程式碼完整給出。
- **Type consistency**：`sortRoomsGrouped` 在 Task 1 定義、Task 2 使用，名稱一致；`sortMode` 值恆為 `'room' | 'name'`，Task 3 開關 `data-sort` 與之對應。